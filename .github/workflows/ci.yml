name: CI/CD Workflow

on: 
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  frontend-tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Clonar repositorio
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Instalar dependencias
        run: npm install

      - name: Run tests with coverage (Vitest)
        run: npx vitest run --coverage 

      - name: Upload coverage report frontend
        uses: actions/upload-artifact@v4
        with:
          name: coverage-frontend
          path: frontend/coverage

  deploy-prod:
    needs: frontend-tests
    if: github.ref == 'refs/heads/main' && needs.frontend-tests.result == 'success'
    runs-on: ubuntu-latest
    environment: Production
    name: Deploy to Production
    
    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Instalar Vercel CLI
        run: npm install -g vercel@latest

      - name: Verificar token de Vercel
        run: |
          if [ -z "${{ secrets.VERCEL_TOKEN }}" ]; then
            echo "❌ ERROR: El token VERCEL_TOKEN no está configurado"
            exit 1
          else
            echo "✅ Token VERCEL_TOKEN encontrado"
          fi

      - name: Deploy a Producción con Vercel CLI
        run: |
          echo "🚀 Iniciando deploy con Vercel CLI..."
          cd frontend  # Cambia al directorio de tu frontend
          
          # Desplegar a producción
          vercel --token ${{ secrets.VERCEL_TOKEN }} --prod --confirm
          
          echo "✅ Deploy completado exitosamente"

      - name: Confirmación final
        run: |
          echo "🎉 FLUJO COMPLETADO EXITOSAMENTE"
          echo "📋 CI: ✅ PASÓ"
          echo "🚀 CD: ✅ DEPLOY COMPLETADO"